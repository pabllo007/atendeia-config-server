server:
  port: 8888
spring:
  application:
    name: atendeia-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pabllo007/atendeia-config-server
          default-label: master
          clone-on-start: true
